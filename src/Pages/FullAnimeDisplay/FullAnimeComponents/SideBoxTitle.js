import React from 'react'

const SideBoxTitle = ({ header }) => {
    return (
        <div className="sideBoxTitle">
            <h3 className="sideAnimeTitle">{header}</h3>
        </div>
    )
}

export default SideBoxTitle
